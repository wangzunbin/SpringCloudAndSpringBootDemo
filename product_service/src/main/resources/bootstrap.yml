#server:
#  port: 8771

#指定注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

# 服务名称
spring:
  application:
    name: product-service
  zipkin:
    base-url: http://192.168.1.49:9411/
  cloud:
    config:
      discovery:
          service-id: config-server
          enabled: true
          #建议用lable去区分环境，默认是lable是master分支
          #label: test
      profile: test

  rabbitmq:
    host: 192.168.1.49
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"